# Public URL of the Django REST API
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_APP_NAME=Smart Compta
# Server-side only. The AI extraction webhook. NEVER exposed to the client.
# (Primary scanner path calls this via Django; the Next /api/webhook bridge also uses it.)
WEBHOOK_URL=https://hook.we.make.com/snccqjyealswqiv3vlwho54oiwtcpb5c
# Optional secret shared with Django (not required for JWT flow)
JWT_SECRET=change-me
